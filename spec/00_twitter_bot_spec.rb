require_relative '../lib/twitter_bot' #fichier .rb

describe "name_method" do
  it "explain_what_should_do_the_method5" do
    expect(methode_name(variables_to_send)).to eq(result_expected)
    expect(methode_name(variables_to_send)).to eq(result_expected)
  end
end
